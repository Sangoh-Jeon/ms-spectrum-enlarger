# 🧪 GLP 질량분석(MS/MS) 피크 글자 확대 웹 프로그램 사용자 매뉴얼

본 프로그램은 GLP 시험 보고서 및 연구 논문에 첨부되는 **질량분석(LC-MS/MS, GC-MS/MS) 스펙트럼 엑셀 파일**의 작은 피크 분자량($m/z$) 텍스트를 보고서 규격에 맞추어 **선명한 대형 글씨(45pt / 35pt)로 자동 변환**해 주는 웹 어플리케이션입니다.

---

## 📌 1. 접속 및 기본 환경

- **웹 앱 접속 URL**:  
  👉 [https://ms-spectrum-enlarger-xj6cjv2jticbvuk5afhaqh.streamlit.app/](https://ms-spectrum-enlarger-xj6cjv2jticbvuk5afhaqh.streamlit.app/)
- **권장 브라우저**: Google Chrome, Microsoft Edge, Whale 등 최신 웹 브라우저
- **설치 필요 여부**: 별도 프로그램 설치 없이 웹 브라우저에서 바로 사용 가능합니다.

---

## 🚀 2. 표준 사용 절차 (Step-by-Step)

```
[1. 엑셀 파일 업로드]
        ↓
[2. Google AI 자동 피크 & 스캔 범위 분석]
        ↓
[3. MRM 조건 이온 선택 및 분자량 확인]
        ↓
[4. '확대 적용 및 파일 생성' 버튼 클릭]
        ↓
[5. 자동 변환된 엑셀 파일 다운로드]
```

### [Step 1] 엑셀 파일 업로드
1. 웹 화면 중앙의 점선 박스(**`📂 분석할 MS/MS 엑셀 파일 (.xlsx, .xls)을 올려주세요`**) 영역으로 Analyst 등 질량분석 장비에서 추출한 엑셀 파일을 드래그 앤 드롭하거나 클릭하여 선택합니다.
2. 업로드가 완료되면 파일명이 초록색으로 표시됩니다.

### [Step 2] AI 자동 분석 및 피크 추출
- 파일이 업로드되면 **Google Gemini AI**가 엑셀 내의 모든 시트(Sheet)와 스펙트럼 이미지를 자동으로 스캔합니다.
- 상단 스캔 범위(`min_mz ~ max_mz`)와 각 피크의 분자량을 자동으로 인식합니다.

### [Step 3] MRM 조건 이온 선택 및 수기 수정 (필요 시)
- 각 시트 탭별로 **Precursor Ion(전구이온)**과 **Product Ion(조각이온)** 피크 목록이 표 형태로 나타납니다.
  - **Precursor Ion**: 기본적으로 가장 높은 주요 피크 1개가 자동 선택됩니다.
  - **Product Ion**: 기본적으로 주요 상위 조각 피크 3개가 자동 선택됩니다.
- **MRM 선택 변경**: 실제 분석법에서 사용한 정량/정성 MRM 이온의 체크박스를 클릭하여 자유롭게 변경할 수 있습니다.
- **분자량 수기 수정**: OCR 인식이 애매하거나 소수점 둘째 자리 미세 조정이 필요한 경우, 표의 **`분자량(m/z)` 열을 더블 클릭하여 직접 수정**할 수 있습니다.

### [Step 4] 변환 및 다운로드
1. 하단의 파란색 **`🚀 MRM 이온 확대 적용 및 엑셀 파일 생성`** 버튼을 누릅니다.
2. 풍선 애니메이션과 함께 변환이 완료되며, 브라우저에서 **`[파일명]_확대.xlsx`** 파일이 **자동으로 다운로드**됩니다.
3. 자동 다운로드가 차단된 경우, 하단의 초록색 **`📥 [파일명]_확대.xlsx 다운로드`** 버튼을 눌러 수동 저장할 수 있습니다.

---

## 🎨 3. 출력 스타일 및 시각적 특징 규격

| 구분 | 서식 규격 | 시각적 특징 | 적용 기준 |
|:---|:---|:---|:---|
| **MRM 선택 이온** | **파란색 45pt (Bold)** | 굵은 파란색 텍스트 | 표에서 체크박스(`MRM 선택`)가 선택된 이온 |
| **기타(Non-MRM) 이온** | **회색 35pt (Regular)** | 은은한 회색 텍스트 | 스펙트럼 상에 존재하는 기타 검출 피크들 |
| **기존 원본 소형 텍스트** | **완전 소거 (Clean)** | 잡음/배경 노이즈 제거 | 파란색 스펙트럼 곡선은 100% 보존 |

---

## 🧠 4. 핵심 지능형 레이아웃 기능

### 1) 100% 결정론적 기하 비례 매핑 (Geometric Scaling)
- 이미지 상단 헤더에 인쇄된 스캔 범위(예: `166 ~ 172 Da`)와 OpenCV로 실측한 플롯 픽셀 경계를 1:1로 결합하여, **피크의 정점(Apex) 위치에 0.1mm 오차도 없이 100% 정확하게 안착**시킵니다.

### 2) 2D 계단식 텍스트 겹침 방지 (Cascading Anti-Collision)
- 인접한 피크들이 다닥다닥 붙어 있어 45pt 대형 글씨끼리 겹치는 경우, **좌측에서 우측으로 순차 검사하여 위쪽으로 계단식 상승**시킵니다.
- 글자가 원래 피크에서 위로 올라간 경우, **원래 피크 꼭대기까지 얇은 지시선(Leader line)과 정점 표시 점이 자동으로 연결**됩니다.

### 3) 상단 고피크(High Peak) 수평 사이드 지시선
- 피크가 그래프 최상단까지 높게 솟아 상단 여백이 부족한 경우, 글자를 **피크 꼭지점의 수평 옆 라인**에 배치하고 **피크 꼭지점을 향해 좌/우 수평 지시선**을 깔끔하게 그어줍니다.

---

## ⚙️ 5. 관리자 설정 및 FAQ

### Q1. 접속하는 동료들이 API 키를 묻지 않고 바로 쓰게 하려면?
- **[Streamlit Cloud 대시보드](https://share.streamlit.io/)** → 해당 앱의 **`Settings` → `Secrets`** 메뉴로 이동합니다.
- 아래 내용을 입력하고 저장해 두면 모든 사용자가 키 입력 없이 100% 자동 분석을 이용할 수 있습니다:
  ```toml
  GEMINI_API_KEY = "AIzaSy실제발급받은키..."
  ```

### Q2. 앱이 잠들어 있을 때(Sleep Mode) 어떻게 하나요?
- 일정 시간 동안 사용자가 없으면 서버가 절전 모드로 들어갑니다.
- 화면 중앙의 파란색 **`Yes, get this app back up!`** 버튼을 누르면 약 30초 내에 정상 재가동됩니다.
